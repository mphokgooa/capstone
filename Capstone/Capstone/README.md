# Capstone — Mpho Kgooa Developer Skillset

This scaffold contains a minimal Django app configured for MariaDB,
Docker (development + production via docker-compose), Sphinx docs, and a git-ready layout.

## Quick notes

- Project name: `capstone_project` (Django project)
- App placeholder: `core`
- Database: MariaDB (configured via environment variables)
- Sphinx docs located in `docs/`
- Use the provided `.env.example` for environment variables.

See `docker-compose.yml` for service definitions.
